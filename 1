#include <stdio.h>
#include <stdlib.h>

// 배열의 최대 크기를 정의
#define MAX 10

// 주어진 배열에서 최대 값을 찾는 함수
int getMax(int array[], int size) {
    int max = array[0];
    for (int i = 1; i < size; i++) {
        if (array[i] > max) {
            max = array[i];
        }
    }
    return max;
}


